import Matches from "./Home";

// `App.jsx` imports `./pages/Matches`, but the existing implementation lives in `Home.jsx`.
// This shim keeps routing working without duplicating the component.
export default Matches;

